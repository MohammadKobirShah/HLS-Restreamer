[railway]
name = "restream-hls"
builder = "docker"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[variableGroups]
production = ["SOURCE_URL", "RTMP_PORT"]

[environment]
PORT = "8080"
NGINX_RTMP_PORT = "1935"
