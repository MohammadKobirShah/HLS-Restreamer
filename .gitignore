# ============================================================
# Cloudflare Tunnel — NEVER commit credentials
# ============================================================
tunnel/credentials.json
tunnel/cert.pem
*.json
!tunnel/config.yml
